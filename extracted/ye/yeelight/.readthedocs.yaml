version: 2

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.10"
  jobs:
    post_install:
      - pip install .
      - pip install -r requirements_dev.txt

sphinx:
  configuration: docs/source/conf.py
